<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.srm.purchasecooperation.cux.pr.infra.mapper.RcwlPrLineHisMapper">

    <select id="selectList" resultType="org.srm.purchasecooperation.pr.domain.entity.PrLine">
        SELECT
            *
        FROM
            ( SELECT * FROM scux_rcwl_pr_line_his srplh WHERE srplh.pr_header_id = #{prHeaderId} AND srplh.tenant_id = #{tenantId}
        ORDER BY srplh.version DESC LIMIT 1 ) temp
        GROUP BY
            temp.pr_header_id
    </select>
</mapper>
